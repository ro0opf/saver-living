export interface LinkItem {
  id: string;
  title: string;
  url: string;
  description?: string;
  image?: string;
}

export interface ProfileData {
  name: string;
  description: string;
  imageUrl: string;
}
